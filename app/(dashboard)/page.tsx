// The full app chrome (sidebar + section switching) lives in ../(dashboard)/layout.tsx's
// DashboardShell. This page is the routed entry for "/"; it renders no extra content.
export default function Page() {
  return null;
}
