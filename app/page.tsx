// The full app chrome (sidebar + section switching) lives in the root layout's
// DashboardShell. This page is the routed entry; it renders no extra content.
export default function Page() {
  return null;
}
